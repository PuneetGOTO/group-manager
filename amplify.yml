version: 1
backend:
  phases:
    build:
      commands:
        - pip install -r requirements.txt
        - pip install gunicorn
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - '/root/.cache/pip/**/*'
